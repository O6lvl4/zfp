name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ziglang/zig:0.15.2
    steps:
      - uses: actions/checkout@v4
      - run: zig build test --summary all
